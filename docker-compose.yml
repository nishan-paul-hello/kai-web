services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kai-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    networks:
      - kai-network

networks:
  kai-network:
    driver: bridge
